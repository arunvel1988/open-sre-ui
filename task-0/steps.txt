#################################################################
sudo apt update -y
sudo apt install nano lsof git -y
sudo apt install docker.io -y
sudo chmod 777 /var/run/docker.sock

docker volume create ollama-data

docker run -d \
  --name ollama \
  -p 11434:11434 \
  -v ollama-data:/root/.ollama \
  --restart unless-stopped \
  ollama/ollama


  docker exec -it ollama ollama pull qwen3:1.7b


  curl -s http://localhost:11434/api/chat \
  -H "Content-Type: application/json" \
  -d '{
    "model": "qwen3:1.7b",
    "messages": [
      {
        "role": "user",
        "content": "Explain Kubernetes in one sentence."
      }
    ],
    "think": false,
    "stream": false
  }'

############################################################################################


# Node.js/npx — needed for the filesystem MCP server
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs

# uv/uvx — needed for the fetch MCP server
curl -LsSf https://astral.sh/uv/install.sh | sh
source $HOME/.local/bin/env   # or just open a new terminal

# verify both are on PATH
node -v && npx -v && uvx --version

###################################################################

  
